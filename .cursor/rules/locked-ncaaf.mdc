# NCAAF UNLOCKED (:5052 / premerge) — results share image only

Owner 2026-09-17: **UNLOCK NCAAF** for bottom Results Image on `/ncaaf-results` (ML / Spread / Total for Last Night + Last 7 — same ad chrome as picks). Owner will check each sport together. Do not change card faces / Books / Consensus for other misses under this unlock.

Prior lock notes preserved below for when this miss is closed.

---

description: NCAAF picks/results UI locked on :5052 — do not edit unless UNLOCK NCAAF
alwaysApply: true
---

# NCAAF LOCKED (:5052 / premerge)

NCAAF picks and results on `:5052` (`predictionlabfix_work2`) are signed off.

Do **not** modify NCAAF unless the owner says:

**UNLOCK NCAAF**

Owner 2026-09-15: unlocked for Grinder2/Takedown N/A, Consensus Historical
Record face chip (replace duplicate H2H), Model Performance win% vs W-L, and
matching checker rules. Miss closed — lock again.

Mercer @ Georgia Tech is an FCS/FBS **moneyline-only** slate (books posted
+347/−445, no spread/total). Do not invent those lines.

Owner 2026-09-13: unlocked for (1) short Mercer/GT card missing Efficiency +
Books spread/total, (2) share/ad card text + 2 picks, (3) chart Spread/Totals
compare table, (4) Best Performing width. Miss closed — lock again.

Owner 2026-09-13: unlocked once to add the shared Cards|Chart results
chart and display the missing results parameters (Efficiency on cards /
Last Night / Last 7 / Season). That miss is closed — lock again.

Fixing WNBA, NFL, MLB, soccer, chrome, or production outages is **not** an
NCAAF unlock.

## Do not touch

- `/ncaaf-picks`, `/ncaaf-results`, `/ncaaf-results?view=chart`
- NCAAF Cards|Chart, card HTML, date nav
- Any `NHL77FINAL.py` / `team_results_charts.py` branch whose job is NCAAF

## Allowed without unlock

- Other sports
- Checker reports that only *read* NCAAF pages

Do not put lock text on user-facing HTML.
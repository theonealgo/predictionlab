# UFC UNLOCKED (:5052 / premerge) — results share image only

Owner 2026-09-17: **UNLOCK UFC** for bottom Results Image on `/ufc-results` (ML / Spread / Total for Last Night + Last 7 — same ad chrome as picks). Owner will check each sport together. Do not change card faces / Books / Consensus for other misses under this unlock.

Prior lock notes preserved below for when this miss is closed.

---

description: UFC picks/results UI locked on :5052 — do not edit unless UNLOCK UFC
alwaysApply: true
---

# UFC LOCKED (:5052 / premerge)

UFC **picks and results** on `:5052` (`predictionlabfix_work2`) are signed off.

Do **not** change `/ufc-picks`, `/ufc-results`, `ufc_live.py`, vendored
`_sandbox_hub_run/hub/ufc_*`, UFC Cards|Chart, share image, or card HTML
unless the owner says **UNLOCK UFC**.

Source of truth for UFC UI: `:5081` hub (`independent_sports/hub`). Premerge copies via HTTP fetch + `_sandbox_hub_run/hub/` vendored modules.

Do not put lock text on user-facing HTML.
# NHL UNLOCKED (:5052 / premerge) — results share image only

Owner 2026-09-17: **UNLOCK NHL** for bottom Results Image on `/nhl-results` (ML / Spread / Total for Last Night + Last 7 — same ad chrome as picks). Owner will check each sport together. Do not change card faces / Books / Consensus for other misses under this unlock.

Prior lock notes preserved below for when this miss is closed.

---

description: NHL LOCKED (:5052 / premerge)
alwaysApply: true
---

# NHL LOCKED (:5052 / premerge)

NHL picks and results on `:5052` (`predictionlabfix_work2`) are signed off.

Do **not** modify NHL unless the owner says:

**UNLOCK NHL**

Owner 2026-09-16: briefly unlocked for Books odds + Consensus Historical
face chip — paused. Owner redirected to CFL header miss first. Lock again.

Fixing CFL, NFL, MLB, soccer, chrome, or production outages is **not** an NHL unlock.

## Do not touch

- `/nhl-picks`, `/nhl-results`, `/nhl-results?view=chart`
- NHL Cards|Chart, card HTML, date nav
- Any `NHL77FINAL.py` / `team_results_charts.py` branch whose job is NHL

## Allowed without unlock

- Other sports
- Checker reports that only *read* NHL pages

Do not put lock text on user-facing HTML.
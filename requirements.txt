# Web framework
flask>=3.1.2
flask-cors
flask-login
gunicorn
ddtrace>=2.14.0

# Auth & payments
authlib
stripe==15.0.1
python-dotenv

# Data
# Pinned to the versions the models were trained/pickled with. Do NOT loosen
# these: unpinned upgrades (e.g. pandas 3.0, numpy 2.4, sklearn 1.9) break
# model loading/prediction at runtime.
pandas==2.3.3
numpy==2.3.4
requests
scipy==1.16.2
scikit-learn==1.7.2
xgboost==3.0.5
catboost==1.2.8
lightgbm==4.6.0

# Sports data (PyPI packages only — local modules like nhl_api.py are in the repo)
nfl-data-py
nhlpy

# Utilities
colorama
tqdm
openpyxl
schedule
google-analytics-data
google-auth
